import React from 'react'

const ClientArea = ({className}) => {
  return (
    <div className={className}>
      <button className="btn m-l-sm">Client Area</button>
      <button className="btn m-l-sm">White label plan</button>
    </div>
  )
}

export default ClientArea
